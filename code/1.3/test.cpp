#include <iostream>
#include <vector>
#include <string>

using namespace std;

struct year_with_name {
    int year;
    string first_name;
};

int main(int argc, char const *argv[])
{
    vector<string> h = {};
    (h.empty() || h.back() != "Lioha");

    return 0;
}
